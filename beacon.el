;; * Beacon Cursor Module -*- lexical-binding: t; -*-
;; Authors:
;;	jgd = J. Greg Davidson

;; ** Dependencies

(ngender-package 'beacon)

;; Setup

(beacon-mode 1)
(setq beacon-push-mark 35)
(setq beacon-color "#666600")

;; Provides

(ngender-provide beacon)
